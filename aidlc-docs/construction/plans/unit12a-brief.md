You are executing **Unit 12, phase 12a: security headers with an enforced content security policy (PR 3)**, in the Portfolio repository at `C:\Users\joshu\source\repos\Portfolio` (Windows 11; use the Bash tool with forward-slash paths; PowerShell only where a .NET command needs it). Work carefully and thoroughly; correctness matters more than speed. You have your own context: everything you need is in the files named below. Do not ask the user questions; make sound engineering choices, follow the plan, and report deviations.

## What phase 12a is

Today the application sets no security header beyond HSTS and cache control, and Kestrel announces itself. This phase adds one pure header composer and one small middleware that put `X-Content-Type-Options`, `Referrer-Policy`, `X-Frame-Options`, `Permissions-Policy`, `Cross-Origin-Opener-Policy` and a content security policy on every response; an env switch `SECURITY_CSP_MODE` (`enforce` default, `report-only`, `off`); a restrictive policy on `/uploads/*`; the Kestrel server header switched off; the theme override `<style>` block allowed by a content hash carried on the theme snapshot; the theme editor's two `style` attributes replaced by CSSOM writes so no component emits a `style` attribute; the unused `ImportMap` component removed; ADR 0003; README and `.env.example`; a security verification document; tests. Rate limiting is phase 12b, not this one.

Read these first, in this order (they are the authority):

1. `aidlc-docs/construction/plans/unit12-security-plan.md`: "Non-negotiables", "What the code looks like today", and the **Phase 12a steps** checklist. Tick each box `[x]` in the same step you complete it with a short "Done: ..." note; leave the review, PR, post-merge and release boxes unticked (they are the orchestrator's). Do not touch the Phase 12b steps.
2. `aidlc-docs/construction/unit12-security/nfr-design/nfr-design-patterns.md` sections 1 to 3 and 7 (how each part works) and `logical-components.md` (where it sits).
3. `aidlc-docs/construction/unit12-security/nfr-requirements/nfr-requirements.md` sections 1, 2, 5 and 6 (the exact header values, the CSP directive by directive, the uploads policy, the mode, the verification) and `tech-stack-decisions.md`.
4. `aidlc-docs/inception/application-design/unit12-security-components.md` section 1 (the parts and the test fixtures for this phase).
5. `aidlc-docs/inception/requirements/requirements-post-unit10-followups.md` section 4.3 (FR-D1 to FR-D8) and section 10.4 (the amendments that override FR-D3 and FR-D7).
6. `docs/adr/0001-first-party-cookieless-analytics.md` and `0002-belt-colors-are-fixed-constants.md` (the ADR style: a title line, one context paragraph, "## Considered Options" with `- **Name** — rejected: reason` bullets, "## Consequences" bullets).
7. The files you change: `src/Portfolio.Web/Program.cs`, `Services/ThemeRules.cs` (the `ThemeSnapshot` record at line 56 and `BuildSnapshot` at 286), `Services/AnalyticsMiddleware.cs` (the neighbour your middleware sits beside; copy its shape), `Components/App.razor` (lines 37 to 49), `Components/Admin/ThemeEditor.razor` (the `style` attributes at 118 and 171, `OnAfterRenderAsync` at 214 where the color-picker module is imported at 221, `SaveAsync` at 318, `DisposeAsync` at 267), `wwwroot/js/colorpicker.js` (exports `init` and `open`), `.env.example`, `README.md` (the env table at about line 79 and "Running behind a reverse proxy" at about 119), `aidlc-docs/construction/build-and-test/unit-test-instructions.md` (today 924 tests, 47 fixtures), a new `aidlc-docs/construction/build-and-test/security-verification-instructions.md`, a new `docs/adr/0003-security-headers-are-emitted-by-the-application.md`.
8. The test conventions: `tests/Portfolio.Tests/ThemeRulesTests.cs`, `NoInlineOnClickTests.cs` and `ResumeLinksTests.cs` (file scans of the linked Razor sources under `RazorComponents/`), `ThemeToggleTooltipTests.cs` (a scan of the linked `js/theme.js`; `colorpicker.js` is not linked yet, add it to `Portfolio.Tests.csproj` with the same `None`/`Link` idiom), `AnalyticsServiceTests.cs` (a `DefaultHttpContext`-style unit test), `SiteContentEditorTests.cs` (the newest scan fixture).

## Branch and hygiene

- The branch `feat/security-headers` ALREADY EXISTS and is checked out, with a `docs:` commit on top of `origin/master` (a6ff7b9, release 1.29.0). Do not create, rebase, reset or switch branches; confirm with `git status -sb` and `git log --oneline -3` and start from the current checkout.
- The orchestrator may append to `aidlc-docs/audit.md` and edit `aidlc-docs/aidlc-state.md` while you work: never stage, stash, revert or edit those two files. You may edit the plan file only to tick your boxes, and include it in your final commit.
- Commit in logical commits (`feat:` the composer, options and middleware with the Program.cs wiring; `feat:` the snapshot hash and the ImportMap removal; `feat:` the theme editor CSSOM colors and the forced reload; `test:` the fixtures; `docs:` the ADR, README, env example, the verification instructions, the test instructions and the plan ticks), staging files explicitly by path (never `git add -A` or `commit -a`). Gate every commit on `dotnet build -warnaserror` at 0 warnings and a `dotnet test` summary containing `Failed:     0,`; chain the commands so a red run stops the commit. Do NOT push and do NOT open a PR.
- Use the Write and Edit tools for every code, test, CSS, JS and config file. The Bash tool on this machine collapses doubled backslashes even inside single quotes, so never generate code through heredocs, printf, sed or perl. Repository files are CRLF; Write and Edit preserve endings; run `unix2dos` on every file you create and confirm with `file`.
- No new NuGet package (`Microsoft.AspNetCore.RateLimiting`, `ForwardedHeaders`, Kestrel options and `SHA256` are all in the shared framework), no script file beyond the edit to `colorpicker.js`, no CDN, no emoji, no inline `onclick`, no `'unsafe-inline'`, no `'unsafe-eval'`, no nonce anywhere, no `style` attribute left in any component, no change to `app.css` tokens. No owner fact anywhere (BR-18). The plain landing page's HTML must not change (NFR-10): headers only, apart from the removed `<ImportMap />` line in the head.
- Never end a process with `taskkill /IM dotnet.exe`; stop processes by PID only.
- A security-guidance plugin is active: pattern notes may follow your edits; judge them on their merits. A JSON `metrics` line with `"skipped": true` after a commit needs no action.

## Implementation notes (the values are pinned by tests; copy them exactly)

- **`Services/SecurityHeadersRules.cs`** (pure, static): `public enum CspMode { Enforce, ReportOnly, Off }`; `ParseCspMode(string?)` maps `report-only` and `off` (trimmed, case-insensitive) and everything else, blank included, to `Enforce`; the constant `PermissionsPolicy = "accelerometer=(), browsing-topics=(), camera=(), display-capture=(), geolocation=(), gyroscope=(), magnetometer=(), microphone=(), midi=(), payment=(), screen-wake-lock=(), usb=(), xr-spatial-tracking=()"`; `BuildCsp(string? styleHash)` returning, joined with `"; "` in this order: `default-src 'self'`, `base-uri 'self'`, `object-src 'none'`, `frame-ancestors 'none'`, `form-action 'self'`, `script-src 'self'`, `style-src 'self'` followed by ` '<styleHash>'` only when a hash is given (the hash value is passed already in the form `sha256-<base64>` and is wrapped in single quotes here), `img-src 'self' blob: https:`, `font-src 'self'`, `connect-src 'self'`, `manifest-src 'self'`; `Compose(CspMode mode, string? styleHash)` returning an ordered `IReadOnlyList<(string Name, string Value)>`: `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `X-Frame-Options: DENY`, `Permissions-Policy`, `Cross-Origin-Opener-Policy: same-origin`, then `Content-Security-Policy` (Enforce) or `Content-Security-Policy-Report-Only` (ReportOnly) with `BuildCsp`, or nothing for Off; `UploadsCsp = "default-src 'none'; style-src 'unsafe-inline'; sandbox"`. XML summaries cite FR-D1, FR-D2, FR-D4, FR-D5 and the NFR requirements.
- **`Services/SecurityOptions.cs`**: `public sealed record SecurityOptions(CspMode CspMode)` with `static SecurityOptions FromConfiguration(IConfiguration config)` reading `SECURITY_CSP_MODE`; registered in `Program.cs` as a singleton next to `SiteConfig` (line 17).
- **`Services/ThemeRules.cs`**: `ThemeSnapshot` gains a trailing `string? OverrideCssHash = null`; `BuildSnapshot` computes `"sha256-" + Convert.ToBase64String(SHA256.HashData(Encoding.UTF8.GetBytes(overrideCss)))` when `overrideCss.Length > 0`, else null. Add a public static `StyleHash(string css)` helper if that keeps `BuildSnapshot` readable and gives the tests one thing to call. The default snapshot carries null.
- **`Services/SecurityHeadersMiddleware.cs`** (beside `AnalyticsMiddleware`): `public sealed class SecurityHeadersMiddleware(RequestDelegate next)` with `public async Task InvokeAsync(HttpContext context, SecurityOptions options, ThemeService themes)`: read `(await themes.GetSnapshotAsync()).OverrideCssHash`, compose the list once, then `context.Response.OnStarting(static state => ..., state)` (or a lambda capturing the list) that, for each pair, sets `context.Response.Headers[name] = value` only when `!context.Response.Headers.ContainsKey(name)`; then `await next(context)`. Summary: why `OnStarting` (it fires when the response begins whatever path the request took and survives the exception handler's reset) and why fill-if-absent (the `/uploads` policy set earlier by the static-file middleware wins). Check how `ThemeService` is registered (singleton or scoped) and inject accordingly; method-parameter injection into `InvokeAsync` handles both.
- **`Program.cs`**: `builder.WebHost.ConfigureKestrel(options => options.AddServerHeader = false);` near the other host setup; `app.UseMiddleware<SecurityHeadersMiddleware>();` immediately after `app.UseForwardedHeaders();` (line 167) with a comment on the placement; in the `/uploads` `StaticFileOptions.OnPrepareResponse` (line 234) add `ctx.Context.Response.Headers["Content-Security-Policy"] = SecurityHeadersRules.UploadsCsp;` and `ctx.Context.Response.Headers["X-Content-Type-Options"] = "nosniff";` next to the existing `Cache-Control`.
- **`Components/App.razor`**: delete the `<ImportMap />` line (48) after confirming `grep -rn "^import\|import(" src/Portfolio.Web/wwwroot/js src/Portfolio.Web/Components --include=*.js` shows only the interop `import()` calls that already receive fingerprinted paths through `JsModuleUrl.Resolve` (no bare specifiers). Leave the `<style>@((MarkupString)_theme.OverrideCss)</style>` line exactly as it is: the hash is computed over that string and the element must contain nothing else. Add one sentence to the existing comment above it saying the block is allowed by the hash in the CSP header (`ThemeSnapshot.OverrideCssHash`) and must stay on one line with no other content.
- **`Components/Admin/ThemeEditor.razor` and `wwwroot/js/colorpicker.js`**: replace `style="@_previewStyle"` with `data-style="@_previewStyle"` and `style="background:@_effective[token.Key]"` with `data-color="@_effective[token.Key]"`; add `@ref="_editorRoot"` (an `ElementReference`) to the component's outermost element; in `OnAfterRenderAsync`, after the module import that happens on the first render, call `await _pickerModule.InvokeVoidAsync("applyDataStyles", _editorRoot)` on every render (guard for a null module and for a disposed circuit the way the file already guards its interop). In `colorpicker.js` add `export function applyDataStyles(root)` that, for every `[data-style]` element under `root` (or `document` when root is missing), sets `element.style.cssText = element.dataset.style`, and for every `[data-color]` element sets `element.style.background = element.dataset.color`; CSSOM writes are not governed by `style-src`. After a successful save (`SaveAsync`, where `_saved = true` is set today), force a full load back to the editor so the new override block and its hash arrive together: inject `NavigationManager` and call `Nav.NavigateTo(Nav.GetUriWithQueryParameter("saved", "1"), forceLoad: true)`; on load, read the query flag through `[SupplyParameterFromQuery(Name = "saved")]` on a `string?` parameter and set `_saved = true` when it is present, so the saved notice still shows. Keep every other behaviour of the editor (the mode toggle, the color picker dialog, the preview) unchanged.
- **Docs**: `.env.example` gets `SECURITY_CSP_MODE=` under a short comment in the file's style (enforce when blank, report-only, off; the other headers are always sent); `README.md` gets an env-table row and a rewritten "Running behind a reverse proxy" section: the app sets its own security headers so a self-hoster without a proxy is covered; a proxy may add headers and need not; duplicates are harmless because every content security policy a browser receives is enforced and the stricter wins, and frame-ancestors supersedes X-Frame-Options; `SECURITY_CSP_MODE=report-only` as the rollout and rollback story with the browser console as the report; `docs/adr/0003-security-headers-are-emitted-by-the-application.md` in the ADR style with the proxy facts (the owner's proxy adds `frame-ancestors 'self'` and `SAMEORIGIN`), the hash-not-nonce decision (enhanced navigation keeps the first response's policy) and the rejected options (headers at the proxy only; a per-request nonce; `'unsafe-inline'` for the admin pages).
- **`aidlc-docs/construction/build-and-test/security-verification-instructions.md`** (new): the curl header checks on `/`, an upload path and a missing page (`curl -sI`), what each header must read; the browser walk with the console open under `enforce` of `/` under both flavors, a blog post with code and its comment section, `/contact` submit, `/signin`, `/feed.xml`, the 404 page, enhanced navigation between pages and a full reload, expecting zero CSP reports; the owner's production checklist under `report-only`: `/admin/theme` (swatches, the color picker, the mode toggle, a save and the reload it triggers), `/admin/site` (an upload and the crop editor), `/admin/stats`, `/admin/posts`, each with the console open, reporting zero reports; then the flip to `enforce` (set the env, recreate the container, curl again); the rollback (`report-only` or `off`).
- **Tests**: `SecurityHeadersRulesTests` (every header and every directive pinned by string; the three modes; the hash directive only with a hash; the uploads policy; `ParseCspMode` rows), `SecurityHeadersMiddlewareTests` (a `DefaultHttpContext` through the middleware with a fake `ThemeService` or a real one over a throwing `IDbContextFactory` if the snapshot cache allows it; invoke the registered `OnStarting` callback the way the framework would, or use a `HttpResponseFeature` whose `OnStarting` you can trigger; assert the headers, fill-if-absent, the report-only header name, and `off`), `ThemeRulesTests` additions (`OverrideCssHash` equals the SHA-256 of the override CSS for a known override; null for the default snapshot), `ProgramPipelineTests` (a text scan of `Program.cs` linked into the test output like `app.css`: the security middleware line before `app.UseRouting()`, `AddServerHeader = false` present, the uploads policy set in `OnPrepareResponse`), `NoInlineStyleTests` (every linked `.razor` file has zero `style="` occurrences), `ColorPickerModuleTests` (the linked `colorpicker.js` exports `applyDataStyles`, contains `style.cssText` and `style.background`, and contains no `setAttribute('style'`), `AppRazorTests` (the linked `App.razor` contains the exact one-line style element and no `<ImportMap`). Refresh `unit-test-instructions.md` (heading totals and the rows for the new fixtures under a "Security" area).

## Verification (required)

- **Build and tests**: 0 warnings; all green; record the summary line.
- **Headers with curl** on the local app (the Unit 11 recipe: `docker run -d --name portfolio-csp-check -p 55432:5432 -e POSTGRES_USER=portfolio -e POSTGRES_PASSWORD=placeholder -e POSTGRES_DB=portfolio postgres:17-alpine`, then `dotnet run --project src/Portfolio.Web` with `ConnectionStrings__Default`, `ASPNETCORE_ENVIRONMENT=Development`, `DataProtection__KeysPath` to a temp folder, `SITE_OWNER_NAME="Jane Developer"`, `CONTACT_EMAIL="jane@example.com"`, `SEED_DEMO_DATA=true` if the seeder gives you a post with code and comments (read `DemoSeeder` to see what it seeds), and `SITE_FLAVOR=bjj` for one run with a belt caption and `SITE_FLAVOR` unset for another): `curl -sI` on `/`, on `/no-such-page`, on `/feed.xml` and, if the seeder stores an upload, on an `/uploads/...` path; every header of the composer present with its value; `Server` absent; the uploads path carrying the uploads policy.
- **The CSP in the Browser pane** under `enforce` (the default): open `/`, a blog post with code and its comment section, `/contact` (submit the form with invented values), `/signin`, `/feed.xml`, `/no-such-page`, navigate between them by clicking links (enhanced navigation) and reload one page fully; read the console with the Browser pane's console tool after each and record zero CSP violations. If the theme overrides table is simple to seed with one row through `psql` in the ephemeral container (read `ThemeService` for the table and column names), seed one overridden token, reload `/`, confirm the override block renders, the `style-src` directive carries a `'sha256-...'` value, and the console shows no violation: that is the hash path exercised end to end. Record what you walked and saw in the plan's verification step. If the Browser pane will not cooperate after about twenty minutes, do the curl checks and report the browser walk as "not done" with what you tried.
- Stop the app by PID and remove the container when done.

## Definition of done

- Every Phase 12a box in the plan is ticked except the review, PR, post-merge and release boxes.
- `dotnet build -warnaserror`: 0 warnings. `dotnet test`: all green with the new fixtures counted (924 before this phase).
- `grep -rn "style=" src/Portfolio.Web/Components --include=*.razor` returns nothing; `grep -n "ImportMap" src/Portfolio.Web/Components/App.razor` returns nothing; no `unsafe-inline`, `unsafe-eval` or `nonce` in `src/` outside `SecurityHeadersRules.UploadsCsp`.
- The plain landing page's HTML (no flavor) is unchanged apart from the removed import-map script; the existing render tests stay green.
- Commits on `feat/security-headers`, not pushed, no PR.

## Final report (the orchestrator reads this; the user does not see your transcript)

Reply with: (a) files created, modified, deleted, grouped by area; (b) the exact build and test summary lines and the final test total; (c) the public surface you added (types, members, the env name, the header values as emitted, the CSS/JS functions); (d) every deviation from the plan and the design and why; (e) the verification results: the curl header output for `/` and for an upload, the browser walk with the pages and the console results, the hash path if exercised; (f) anything unsure or left undone; (g) the commit hashes.
