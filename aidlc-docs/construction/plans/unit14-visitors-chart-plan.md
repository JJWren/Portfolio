# Unit 14 plan: daily-visitors chart on the admin stats page (PR B)

**Written**: 2026-09-11. **Owner request (verbatim)**: "I would like to create one more add request. In the admin/stats view, above Top Pages but below the stat blocks, I would like a graph that changes based on the dropdown (7 days, 30 days, 90 days, 365 days) that is a line graph which shows the daily visitors." **Requirements**: `inception/requirements/requirements-admin-stats-visitors-chart.md` (FR-V1 to FR-V13, NFR-22 to NFR-26; approved 2026-09-11). **Workflow slot**: PR B of `inception/plans/workflow-planning-units-13-14.md`, after Unit 13 (merged as PR #103) and ahead of Units 11 and 12. **Glossary**: Daily Visitor, Period, Rollup, Watermark (`CONTEXT.md`). **Gate**: the owner pre-approved this plan and the unit's completion on 2026-09-11 ("Continue through with the requests from this session until complete and then deploy"), so the gates are recorded in the audit trail rather than paused at.

**Execution note**: the workflow plan's per-PR cycle applies: one fresh Sonnet phase agent briefed from this plan (`unit14-brief.md`), five review agents, remediation, the Copilot gate, squash-merge. The phase agent loads the dataviz skill before writing chart code and runs its palette validator. The Components section is the unit's Application Design (minimal).

## Non-negotiables

- Inline SVG rendered by Razor from the data. No script, no CDN, no external resource, no new package (NFR-1, NFR-2, NFR-22).
- One series, daily visitors, zero baseline, no legend; the heading names it. Never a second axis.
- Colors from tokens only: the series token validated on both surfaces with the dataviz validator; text and axes in text and border tokens; nothing in `:root` changes and the 26-token theme catalog and its pinned tests stay untouched.
- No animation or transition anywhere in the chart (NFR-6); nothing `position: fixed`.
- The points sum to the tile's Daily visitors figure for the same Period (FR-V2); the chart and the tiles read the same rows.
- No owner fact in code, tests or fixtures (BR-18): test data is invented.

## Components (Application Design, minimal)

| Component | Kind | Members and behaviour | Depends on |
|---|---|---|---|
| `Services/AnalyticsService.cs` (changed) | service | `public record DailyVisitorPoint(DateOnly Day, int Visitors, bool IsToday);` and `GetDailyVisitorsAsync(DateOnly from, DateOnly to)` returning one point per day from `from` to `to` inclusive, in date order: the rolled-up `DailySiteStats` row's `Visitors` for each day (a day without a row is 0), and when the range includes today, today's value computed live exactly as `GetSummaryAsync` does (distinct `VisitorKey` among page views since midnight UTC), flagged `IsToday` | `AppDbContext`, `TimeProvider` |
| `Services/VisitorsChartRules.cs` (new, static, pure) | geometry and rules | `MinPoints` (2); `RangeStart(DateOnly periodStart, DateOnly firstRecordedDay)` (the later of the two); `NiceMax(int max)` (the smallest round number of the form 1, 2, 5 times a power of ten that is at least `max`, and 1 when `max` is 0); `Ticks(int niceMax)` (three to five evenly spaced round values from 0 to `niceMax`); a `ChartLayout` record with the viewBox size (720 by 240), the plot inset (left 44, right 12, top 12, bottom 28) and the plot width and height; `X(int index, int count)` and `Y(int value, int niceMax)` mapped into the plot; `LinePath(IReadOnlyList<int> values, int niceMax)` (the `M`/`L` path through every point except that the final segment is omitted when the last point is today); `TodayPath(...)` (the dashed final segment); `LabelIndexes(int count, int target = 5)` (indexes for the date labels, always including the first and last, evenly spaced, no duplicates); `FormatDay(DateOnly day)` ("14 Aug", invariant culture); `TooltipAnchor(int index, int count)` (start anchor for the first half of the days, end anchor for the second half, so the tooltip stays inside the frame) | nothing |
| `Components/Admin/VisitorsChart.razor` (new) | admin component | `[Parameter] IReadOnlyList<DailyVisitorPoint> Points` (required). Renders, inside a `.card.visitors-chart`: an h2 "Daily visitors" and the muted caption "one point per UTC day"; with fewer than `MinPoints` points the message "Not enough data to chart yet." and nothing else; otherwise an `<svg viewBox="0 0 720 240" role="img" aria-labelledby="<heading id>">` with the gridlines and y labels, the x labels, the line path, the dashed today segment and its hollow marker, then one `<g class="day" tabindex="0">` per day carrying an invisible hit `<rect>` the full plot height and one day wide (at least 6 units wide), an `aria-label` "14 Aug: 12 visitors" (today: "14 Aug, today so far: 12 visitors") and a hidden `<g class="tip">` with a crosshair line, a marker circle and a labelled box; then `<details class="visitors-data"><summary>Data table</summary>` with a table of every day and its visitors | `VisitorsChartRules`, `DailyVisitorPoint` |
| `Components/Admin/Stats.razor` (changed) | admin page | after the tiles and before the Top pages heading: `<VisitorsChart Points="_dailyVisitors" />`; `_dailyVisitors` loaded in the existing `LoadAsync` from `Analytics.GetDailyVisitorsAsync(VisitorsChartRules.RangeStart(from, await Analytics.FirstDayAsync()), to)`; the period select is unchanged | `AnalyticsService`, `VisitorsChartRules` |
| `wwwroot/app.css` (changed) | styles | a "Visitors chart" block in the admin stats area: the svg at full width with `height: auto` and a minimum height on narrow screens; `.grid line` in `--border`; axis text in `--text-muted` at a small SVG font size; `.line` stroke in the chosen series token, 2px, no fill, round joins; `.today` the same token with a dash array; `.marker` filled with `--surface` and stroked with the token; `.day .hit` transparent fill; `.day .tip` hidden by default and shown on `.day:hover` and `.day:focus`; `.tip rect` filled with `--surface` and stroked with `--border`; `.tip text` in `--text`; a visible focus ring on `.day:focus-visible .hit`; no transition anywhere. The series token is `--info` unless the validator prefers `--c-blue` (both are recorded in the brief with the surface values) | tokens |
| Tests | fixtures | `VisitorsChartRulesTests` (RangeStart later-of-two both ways; NiceMax for 0, 1, 3, 12, 49, 120, 1000; Ticks are three to five evenly spaced values ending at niceMax; X and Y stay inside the plot; LinePath and TodayPath for three points with and without today; LabelIndexes for 2, 7, 30, 365 days include the first and last and never repeat; FormatDay invariant; TooltipAnchor by half); `VisitorsChartRenderTests` (HtmlRenderer with fixed points, following `LandingSectionsRenderTests`: heading and caption present; one point gives the empty message and no svg or table; seven points give seven `g.day` groups, seven table rows with the right numbers, the dashed today segment only when the last point is today, an `aria-label` per day, no `<script` and no `http` inside the component); one `AppCssTests` pin (the chart rules exist, `.day .tip` is hidden and shown on hover and focus, and none of the chart rules declares a transition or animation) | `LandingRenderHarness`, `CssScanner` |

## What the code looks like today (evidence)

- `Stats.razor` (`@rendermode InteractiveServer`): the period select (7, 30, 90, 365, all) binds `_period` and calls `ReloadAsync`; `LoadAsync` computes `(from, to)` through `PeriodRangeAsync` (today minus N minus 1, or `FirstDayAsync` for all) and loads the summary, the routes page, the referrers and the events; the tiles grid (`.stat-tiles`) ends with `margin: 1.5rem 0 2rem` and the "Top pages" h2 follows.
- `AnalyticsService.GetSummaryAsync` sums `DailySiteStats` rows in range and adds today's live counts (`PageViews` since midnight UTC, distinct `VisitorKey`); `FirstDayAsync` returns the earliest rolled-up day or today.
- `DailySiteStat` has `Day`, `Views`, `Visitors`; one row per rolled-up day including zero days (the Watermark).
- Tokens: dark `--bg #151515`, `--surface #1d1c1a`, `--border #35322d`, `--text #e8e4dd`, `--text-muted #a49d91`, `--info #8fb6c9`, `--c-blue #6494aa`; light `--bg #f6f3ec`, `--surface #fdfbf6`, `--border #d8d1c2`, `--text #24211c`, `--text-muted #6b6459`, `--info #3f6e86`, `--c-blue` unchanged.
- Render tests use `LandingRenderHarness` (HtmlRenderer plus a service collection) and `AppCssTests` parse `app.css` leaf rules through `CssScanner`. The dataviz validator is `scripts/validate_palette.js` in the skill folder; node 22 is installed.
- No development sign-in exists, so `/admin/stats` cannot be driven locally through the UI; the chart component is rendered in tests with fixed data, and the "render and look" step runs the component in a throwaway page or the local app with synthetic rows (the brief says how).

## Steps

- [x] Branch `feat/admin-stats-visitors-chart` from a realigned master (stash-first); first commit `docs:` folding every pending aidlc-docs edit (audit, state, plans, this plan, the brief).
- [x] Load the dataviz skill; run the validator for the candidate series tokens against both surfaces; record the report and the chosen token in this plan.

  **Validator reports** (`node scripts/validate_palette.js "<hex>" --mode <dark|light> --surface "<surface>"`, run from the dataviz skill's base directory; each candidate is a single-color, one-series palette, so the categorical checks calibrated for telling *multiple* series apart — Lightness band and Chroma floor — are the ones the script's own output labels out of scope ("scope: categorical palettes only. For a lone status/text color check WCAG text contrast..."); Contrast vs surface is the check that actually applies to a lone series line, and both candidates pass it on both surfaces):

  | Candidate | Surface | Lightness band | Chroma floor | Contrast vs surface | Verdict |
  |---|---|---|---|---|---|
  | `--info` `#8fb6c9` | dark `#1d1c1a` | FAIL — outside band (L 0.754, band 0.48-0.67) | FAIL — 0.05 < 0.10 floor | PASS — >= 3:1 | FAILED (2 checks) |
  | `--info` `#3f6e86` | light `#fdfbf6` | PASS — inside band 0.43-0.77 | FAIL — 0.064 < 0.10 floor | PASS — >= 3:1 | FAILED (1 check) |
  | `--c-blue` `#6494aa` | dark `#1d1c1a` | PASS — inside band 0.48-0.67 | FAIL — 0.061 < 0.10 floor | PASS — >= 3:1 | FAILED (1 check) |
  | `--c-blue` `#6494aa` | light `#fdfbf6` | PASS — inside band 0.43-0.77 | FAIL — 0.061 < 0.10 floor | PASS — >= 3:1 | FAILED (1 check) |

  **Chosen token: `--c-blue`.** The brief says to choose `--info` unless the report prefers `--c-blue`; here it does. `--c-blue` passes Lightness band on both surfaces, while `--info` also fails Lightness band on the dark surface (badly: L 0.754 is above the 0.67 ceiling, since `--info`'s dark value is tuned to read as light body/icon text on a dark surface, not as a 2px line stroke) in addition to Chroma floor. On the light surface the two candidates are identical on every check (light `--info` and `--c-blue` are both mid-toned blues with near-identical chroma, 0.064 vs 0.061). Both candidates fail the Chroma floor check on every surface by a small margin — expected and accepted per the plan's non-negotiables ("A contrast WARN is acceptable because the chart has visible labels and a table"): Chroma floor is a categorical-palette check (is this hue distinguishable from a *neighboring series*), moot for the chart's one series, and the check the requirement actually cares about (FR-V7: legible on both surfaces) is Contrast vs surface, which both candidates pass on both surfaces at >= 3:1. `--c-blue` is therefore the strictly-better (never worse) candidate across every check on every surface, so it is the one used in `app.css`.
- [x] `Services/VisitorsChartRules.cs` and `Services/AnalyticsService.cs` (`DailyVisitorPoint`, `GetDailyVisitorsAsync`).
- [x] `Components/Admin/VisitorsChart.razor`; `Components/Admin/Stats.razor` wiring; `wwwroot/app.css` chart block.
- [x] Tests: `VisitorsChartRulesTests`, `VisitorsChartRenderTests`, the `AppCssTests` pin; `construction/build-and-test/unit-test-instructions.md` refreshed (two new fixtures, new totals).
- [x] `dotnet build -warnaserror` with 0 warnings; `dotnet test` green (gate on the summary line).
- [x] Render and look: the chart at 7, 30, 90, 365 and All time and the empty state, in both themes, checked for label collisions, tooltip clipping and the dashed today segment; the brief gives the method (a throwaway static page rendered from the component, or the local app with synthetic rows inserted into an ephemeral Postgres). Findings fixed before the review.
- [ ] Five-area review (report-only: correctness, security including the BR-18 grep, framework, maintainability, performance) and remediation; build and tests green after every applied finding.
- [ ] Push; PR `feat: daily-visitors chart on the admin stats page`; Copilot gate per CONTRIBUTING.md; squash-merge; realign master stash-first; delete the branch; tick this plan, the state file, the audit and memory.
- [ ] After the merge: merge the release-please PR, wait for the image publish, bump the compose tag in the production folder and recreate the container, check the live site (owner instruction 2026-09-11); the admin stats page check itself stays the owner's.

## Definition of done

The stats page shows a daily-visitors line between the tiles and Top pages that follows the period select, includes today as a dashed final segment, starts no earlier than the first recorded day, has a hover and focus tooltip and a data table, shows the empty state under two days, uses tokens validated on both surfaces, contains no script, and sums to the tile; build and tests green; PR merged with a clean Copilot pass.
