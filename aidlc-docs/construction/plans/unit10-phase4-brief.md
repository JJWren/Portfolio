You are executing **Phase 4 of Unit 10** in the Portfolio repository at `C:\Users\joshu\source\repos\Portfolio` (Windows 11; use the Bash tool with forward-slash paths; PowerShell only where a .NET command needs it). Work carefully and thoroughly; correctness and fidelity to the approved design matter more than speed. You have your own context: everything you need is in the files named below. Do not ask the user questions; make sound engineering choices, follow the plan, and report deviations.

## What Phase 4 is

The `feat:` phase that gives the site a **second owner portrait slot** (the "mat" photo) through the existing `OwnerPhotoService`, and, under the BJJ landing flavor, shows the hero portrait as a **two-photo switch** (a 4:5 photo stack with a two-radio pill switch under it, "At the desk" / "On the mat", CSS `:has(:checked)` crossfade, none under reduced motion). With only one photo the hero renders exactly today's single `<img class="owner-photo">`. The phase also adds `fetchpriority="high"` to the primary hero image. Phase 5 (perf close-out and docs) is not yours.

Read these first, in this order (they are the authority; where this brief and a file disagree, the plan and the functional design win):

1. `aidlc-docs/construction/plans/unit10-bjj-landing-plan.md`: "Non-negotiables", "Design refinements made in Functional Design", and the **Phase 4** checklist (your task list; tick each box `[x]` in the same step you complete it, leaving "Gate" and "Owner actions after deploy" unchecked). Phases 1 to 3 are merged.
2. `aidlc-docs/construction/unit10-bjj-landing/functional-design/domain-entities.md` (sections 1 and 4) and `business-rules.md` (BR-1, BR-3, BR-11, BR-13, BR-14, BR-17, BR-18, BR-19).
3. `aidlc-docs/construction/plans/unit10-bjj-landing-handoff.md`: "The approved design" item 2 (the hero) and the locked decisions 3 and 5. The photo file names and alt texts written there are the owner's values: content only, never in code, tests or `.env.example` (BR-18).
4. The design sources, which are the exact markup and CSS to copy: `aidlc-docs/inception/application-design/landing-directions/parts/Main.body.html` lines 15 to 25 (the `.photo-slot`, `.photo-stack`, the two `.photo-tile` images, the `pill-switch photo-switch` fieldset), `parts/Main.css` (`.photo-slot`, `.photo-stack`, `.photo-tile`, `.photo-tile.mat`, the two `.hero:has(#ph-mat:checked)` rules, `.photo-switch`, `.photo-tile img`, and the three `.photo-*` rules inside its 720px block; skip the `.ph-art` placeholder-art rules, they are canvas-only), `parts/shared.css` lines 137 to 141 (`.pill-switch` and its `input`, `label`, `:checked`, `:focus-visible` rules), and `parts/Lead.css` (`.hero .container.has-photo .photo-slot { align-self: start; }`). Copy values from these files, never from memory. Do not edit anything under `landing-directions/`.
5. What you extend: `src/Portfolio.Web/Services/OwnerPhotoService.cs` (today one slot: `IsConfigured` property, `GetVersionedUrl()`, `SaveAsync(stream)`, `Delete()`, `SniffContentType`), `Endpoints/SeoEndpoints.cs` (the `/owner-photo` handler, roughly lines 48 to 80), `Services/SiteConfig.cs` (`OwnerPhotoFile`, `OwnerPhotoAlt` and `NullIfEmpty`), `Services/SiteContentRules.cs` (`EffectiveSiteContent.OwnerPhotoAlt`, `SiteContentDraft.OwnerPhotoAlt`, `OwnerPhotoAltMaxLength`, `CheckLengths`, `Validate`, `Resolve` line about 304), `Services/SiteContentService.cs`, `Data/SiteContent.cs`, `Data/AppDbContext.cs`, `Migrations/20260904182920_AddRoadAndNow.cs` (the pattern), `Components/LandingSections.razor` (the hero: `has-photo` container, the single `<img class="owner-photo">`, `_photoUrl`, `OnInitialized`), `Components/Admin/SiteContentEditor.razor` (the "Owner photo" group, upload, remove, preview, alt field, roughly lines 110 to 155 and 175 to 260), `Components/Pages/Home.razor` (JSON-LD keeps `/owner-photo`; do not change, BR-19), `wwwroot/app.css` (the `Landing (BJJ flavor)` banner section, its single 720px block, and the single `@media (prefers-reduced-motion: reduce)` block whose declarations all carry `!important`), `.env.example`, and the tests `tests/Portfolio.Tests/OwnerPhotoServiceTests.cs`, `SiteConfigTests.cs`, `SiteContentRulesTests.cs`, `LandingSectionsRenderTests.cs`, `AppCssTests.cs`, `Support/LandingRenderHarness.cs` (`MaximalConfig`/`MaximalContent` must be extended again, per their own doc comment), `Support/CssScanner.cs`.

## Branch and hygiene

- The branch `feat/owner-photo-flip` ALREADY EXISTS and is checked out, with one `docs:` commit on top of `origin/master`. Do not create, rebase, reset or switch branches; confirm with `git status -sb` and `git log --oneline -3` and start from the current checkout.
- The orchestrator may append to `aidlc-docs/audit.md` and edit `aidlc-docs/aidlc-state.md` while you work: never stage, stash, revert or edit those two files. You may edit the plan file only to tick your Phase 4 boxes, and include it in your final commit.
- Commit in logical commits (service and endpoint, data and rules, editor, UI and CSS, tests); the branch must end with `dotnet build -warnaserror` at 0 warnings and `dotnet test` fully green. Stage files explicitly by path (never `git add -A` or `commit -a`). Do NOT push and do NOT open a PR.
- No new NuGet packages. No `@rendermode` on anything public. No inline `onclick`. No JS. No CDN, no emoji, no icon font. Nothing `position: fixed` (the `.pill-switch input { position: absolute; ... }` rule from the design is fine).
- Never end a process with `taskkill /IM dotnet.exe`; if you run the site locally, stop it by PID only.
- A security-guidance plugin is active: pattern notes may follow your edits; judge them on their merits. A JSON `metrics` line with `"skipped": true` after a commit needs no action.

## Implementation notes the plan does not spell out

- **`OwnerPhotoSlot`** enum in `OwnerPhotoService.cs`: `Primary` (the desk photo, today's slot), `Flip` (the mat photo). Every service member takes a slot defaulting to `Primary`: `bool IsConfigured(OwnerPhotoSlot slot = OwnerPhotoSlot.Primary)` (today a property; C# cannot keep both, so make it a method and update its two call sites in the editor to `Photo.IsConfigured()`), `string? GetVersionedUrl(OwnerPhotoSlot slot = Primary)`, `Task SaveAsync(Stream source, OwnerPhotoSlot slot = Primary, CancellationToken cancellationToken = default)`, `void Delete(OwnerPhotoSlot slot = Primary)`. A private `PathFor(slot)` returns `site.OwnerPhotoFile` or `site.OwnerPhotoFlipFile`; the URL is `/owner-photo?v=` for `Primary` and `/owner-photo-flip?v=` for `Flip` (write-time ticks as today). The unconfigured exception message must name the right variable (`OWNER_PHOTO_FILE` or `OWNER_PHOTO_FLIP_FILE`). Existing tests must compile and pass unchanged.
- **Endpoint**: in `SeoEndpoints.cs`, extract the `/owner-photo` body into one handler that takes the file path (or the slot) and map both `/owner-photo` (primary) and `/owner-photo-flip` (flip) to it; identical sniffing, 404 on unconfigured or missing, identical caching semantics (versioned request immutable, bare request must revalidate). Keep the existing comments' intent.
- **`SiteConfig`**: trailing optional `OwnerPhotoFlipFile` and `OwnerPhotoFlipAlt` (`string?`, `NullIfEmpty`) from `OWNER_PHOTO_FLIP_FILE` and `OWNER_PHOTO_FLIP_ALT`.
- **`SiteContent.OwnerPhotoFlipAlt`** (`string?`, max 200 like `OwnerPhotoAlt`); migration `AddOwnerPhotoFlipAlt` generated with `dotnet tool restore` then `dotnet ef migrations add AddOwnerPhotoFlipAlt --project src/Portfolio.Web --startup-project src/Portfolio.Web` (placeholder `SITE_OWNER_NAME`, `CONTACT_EMAIL` and `ConnectionStrings__Default` environment values if the design-time host needs them). Review `Up`/`Down` and the snapshot diff: only the one column may change.
- **`SiteContentRules`**: `SiteContentDraft` gains `OwnerPhotoFlipAlt` (append it after the existing fields the way Phase 3 appended its fields; update the single construction site in the editor); `EffectiveSiteContent.OwnerPhotoFlipAlt` resolved exactly like `OwnerPhotoAlt` (override, then `site.OwnerPhotoFlipAlt`, then `$"Portrait of {site.OwnerName}"`), never null; `Validate` checks its length against `OwnerPhotoAltMaxLength` with a message like the existing alt-text one but saying "Mat photo alt text"; keep the constructor backward compatible the way Phases 2 and 3 did. `SiteContentService` upserts the new column.
- **Editor**: after the existing "Owner photo" group, a "Mat photo" group with the same structure (preview, upload through `InputFile` calling `Photo.SaveAsync(stream, OwnerPhotoSlot.Flip)`, remove calling `Photo.Delete(OwnerPhotoSlot.Flip)`, busy flag) and a "Mat photo alt text" field bound to the new draft field with the env placeholder, all shown only when `Photo.IsConfigured(OwnerPhotoSlot.Flip)`; when it is not configured, show the same kind of hint as the primary group but naming `OWNER_PHOTO_FLIP_FILE`. Keep the primary group byte-for-byte apart from the `IsConfigured()` call change.
- **`LandingSections.razor`**: keep `has-photo` keyed on the primary URL. Under `Bjj`, when both `Photo.GetVersionedUrl()` and `Photo.GetVersionedUrl(OwnerPhotoSlot.Flip)` are non-null, render in the hero, in place of the single image:
  `<div class="photo-slot"><div class="photo-stack"><div class="photo-tile desk"><img class="owner-photo" src="{primary}" alt="{alt}" fetchpriority="high" /></div><div class="photo-tile mat"><img src="{flip}" alt="{flipAlt}" loading="lazy" /></div></div><fieldset class="pill-switch photo-switch"><legend class="visually-hidden">Show photo</legend><input type="radio" name="photo" id="ph-desk" checked /><label for="ph-desk">At the desk</label><input type="radio" name="photo" id="ph-mat" /><label for="ph-mat">On the mat</label></fieldset></div>`
  Alt texts go through `SeoRules.NormalizeAltText` like today. In every other case (Default flavor, or only one photo resolves) render today's single `<img class="owner-photo" ...>` unchanged apart from adding `fetchpriority="high"`; that attribute is required by the plan for the primary hero image in both cases, so a Default-flavor render assertion that pins the exact `<img ...>` string may be updated for that one attribute and nothing else. Resolve both URLs once in `OnInitialized`.
- **`app.css`**: append to the `Landing (BJJ flavor)` banner section, in the file's multi-line style: `.pill-switch` and its four sub-rules (from `shared.css`), `.photo-slot`, `.photo-stack`, `.photo-tile`, `.photo-tile.mat`, `.hero:has(#ph-mat:checked) .photo-tile.mat`, `.hero:has(#ph-mat:checked) .photo-tile.desk`, `.photo-switch`, `.photo-tile img` (from `Main.css`), and `.hero .container.has-photo .photo-slot { align-self: start; }` (from `Lead.css`). Add the three mobile rules (`.photo-slot { order: -1; justify-self: center; width: min(60vw, 260px); }`, `.photo-stack { margin-top: 2rem; width: 100%; }`, `.photo-switch { justify-content: center; }`) to the section's EXISTING single 720px block (do not open a second one). Add `.photo-tile { transition: none !important; }` to the single existing reduced-motion block (every declaration there carries `!important`; the `AppCssTests` facts enforce both the listing and the `!important`).
- **`.env.example`**: `OWNER_PHOTO_FLIP_FILE=` and `OWNER_PHOTO_FLIP_ALT=` next to the primary pair, with comments mirroring theirs (same read-write mount, generic alt example such as `Portrait of the site owner on the mat`).

## Tests to write (extend, do not duplicate)

- `OwnerPhotoServiceTests`: unconfigured flip slot (`IsConfigured(Flip)` false, `GetVersionedUrl(Flip)` null, `SaveAsync(..., Flip)` throws naming `OWNER_PHOTO_FLIP_FILE`); URL per slot (`/owner-photo?v=` vs `/owner-photo-flip?v=`); save and delete per slot; a flip save leaves the primary file's bytes and write time untouched; every existing test unchanged.
- `SiteConfigTests`: the two new variables read and empty means null.
- `SiteContentRulesTests`: `OwnerPhotoFlipAlt` resolve precedence (override, env, "Portrait of {owner}"), `Validate` length error, existing tests untouched.
- `LandingSectionsRenderTests`: under `Bjj` with both files: the fieldset with the visually hidden legend "Show photo", `id="ph-desk"` checked and `id="ph-mat"` unchecked, both alt texts encoded, `fetchpriority="high"` on the desk image only, `has-photo` present; under `Bjj` with only the primary file: the single image and no fieldset; under `Default` with both files: the single image and no fieldset; no `position: fixed`, no `<script`. Extend `MaximalConfig` with a flip file (a real temp file, as the harness does for the primary) so the fixed-position cross-check covers the switch markup. Fixtures stay invented (BR-18).
- `AppCssTests`: `.photo-tile` appears in the reduced-motion block with `!important` (the existing coverage facts should demand it; add a targeted fact only if none fails without the line); exactly one 720px block still inside the `Landing (BJJ flavor)` banner.

## Local visual check (do it if you can within reasonable effort; otherwise say so)

The plan asks for the crossfade, keyboard operation of the radios (Tab to the group, arrow keys switch, focus ring visible), reduced motion, and the admin preview frame at rest. If you can run the site locally (Phase 3 did it with `dotnet run` against an ephemeral, isolated Postgres container on a throwaway port with sample `SITE_*` and two temporary photo files for the process only), do it and report what you saw; do not commit any `.env` change and do not spend more than about fifteen minutes on environment problems; stop processes by PID and clean up containers, temp files and browser tabs afterwards. Reduced-motion emulation and admin OAuth are not available locally; say so where they apply.

## Definition of done

- Every Phase 4 box in the plan is ticked except "Gate" and "Owner actions after deploy".
- `dotnet build -warnaserror`: 0 warnings, 0 errors. `dotnet test`: all green; record the summary line and the new total (666 before this phase).
- The `Default` flavor renders as before apart from `fetchpriority="high"`; Phase 2 and 3 sections untouched (existing tests green).
- Commits on `feat/owner-photo-flip`, not pushed, no PR.

## Final report (the orchestrator reads this; the user does not see your transcript)

Reply with: (a) files created, modified, deleted, grouped by area; (b) the exact build and test summary lines and the new test total; (c) the migration name and the column it adds; (d) the public surface you added or changed (so Phase 5 can be briefed), including any call-site changes; (e) every deviation from the design files or the plan and why; (f) the result of the visual check or "not done"; (g) anything unsure or left undone; (h) the commit hashes.
