You are executing **Phase 5 of Unit 10**, the close-out phase, in the Portfolio repository at `C:\Users\joshu\source\repos\Portfolio` (Windows 11; use the Bash tool with forward-slash paths; PowerShell only where a .NET command needs it). Work carefully and thoroughly; correctness matters more than speed. You have your own context: everything you need is in the files named below. Do not ask the user questions; make sound engineering choices, follow the plan, and report deviations.

## What Phase 5 is

The `perf:` close-out of the BJJ landing redesign: preload the display font, replace the four inline `onclick` attributes with one delegated click handler, and bring the documentation up to date with everything Phases 2 to 4 shipped (README, the `CONTEXT.md` glossary, the unit-test instructions). Phases 1 to 4 are merged (PRs #89, #90, #92, #94). This is the last phase of the unit.

Read these first, in this order (they are the authority):

1. `aidlc-docs/construction/plans/unit10-bjj-landing-plan.md`: "Non-negotiables" and the **Phase 5** checklist (your task list; tick each box `[x]` in the same step you complete it; leave the "Gate" box and the `aidlc-state.md`/`audit.md`/memory box unchecked, those are the orchestrator's; the "Optional, owner's call" box is deferred, see below).
2. `aidlc-docs/construction/plans/unit10-bjj-landing-handoff.md` ("Constraints") and `aidlc-docs/construction/unit10-bjj-landing/functional-design/domain-entities.md` and `business-rules.md` (for the glossary wording; BR-18 still applies: no owner facts in code, tests, `.env.example` or docs examples).
3. The files you change: `src/Portfolio.Web/Components/App.razor` (the `<head>`: `<ResourcePreloader />`, `theme.js`, `app.css`), `src/Portfolio.Web/wwwroot/app.css` lines 1 to 27 (the three `@font-face` rules; the display font is `Fraunces` at `fonts/fraunces-latin.woff2`), `src/Portfolio.Web/wwwroot/js/site.js` (the IIFE that exports `window.__toggleNav` and `window.__scrollProjects` at the end, and calls `window.__applyTheme` on enhanced load), `src/Portfolio.Web/wwwroot/js/theme.js` (exports `window.__applyTheme` and `window.__toggleTheme`; runs in the head before first paint; leave its behaviour as is), `src/Portfolio.Web/Components/Layout/MainLayout.razor` (the `.nav-burger` button with `onclick="__toggleNav(this)"` and the `.theme-toggle` button with `onclick="__toggleTheme()"`), `src/Portfolio.Web/Components/Pages/Projects.razor` (two `.carousel-btn` buttons with `onclick="__scrollProjects(-1)"` and `onclick="__scrollProjects(1)"`), `README.md` ("Features", "Configuration" and its env table), `CONTEXT.md` ("## Language", "### Site identity" with the existing `**Owner Photo**:` entry and the `_Avoid_:` line format), `aidlc-docs/construction/build-and-test/unit-test-instructions.md` (the "Coverage" table that says it predates several fixtures and is reconciled in this phase), `.env.example` (read only, for the exact env names and their comments).
4. What the previous phases added, for the docs: `src/Portfolio.Web/Services/SiteFlavor.cs`, `Services/BjjRules.cs`, `Services/SiteConfig.cs`, `Services/OwnerPhotoService.cs`, `Endpoints/SeoEndpoints.cs`, `Components/LandingSections.razor`, `Components/GamePlan.razor`, `RankBar.razor`, `BeltBand.razor`, `Road.razor`, `Components/Admin/SiteContentEditor.razor`, `docs/adr/0002-belt-colors-are-fixed-constants.md`.

## Branch and hygiene

- The branch `perf/landing-closeout` ALREADY EXISTS and is checked out, with one `docs:` commit on top of `origin/master`. Do not create, rebase, reset or switch branches; confirm with `git status -sb` and `git log --oneline -3` and start from the current checkout.
- The orchestrator may append to `aidlc-docs/audit.md` and edit `aidlc-docs/aidlc-state.md` while you work: never stage, stash, revert or edit those two files. You may edit the plan file only to tick your Phase 5 boxes, and include it in your final commit.
- Commit in logical commits (perf: font preload and delegated handlers; docs: README and CONTEXT; docs: test instructions); the branch must end with `dotnet build -warnaserror` at 0 warnings and `dotnet test` fully green. Stage files explicitly by path (never `git add -A` or `commit -a`). Do NOT push and do NOT open a PR.
- No new NuGet packages. No `@rendermode` on anything public. No CDN, no emoji, no icon font. No owner facts anywhere (BR-18): README and glossary examples stay generic.
- Never end a process with `taskkill /IM dotnet.exe`; stop processes by PID only.
- A security-guidance plugin is active: pattern notes may follow your edits; judge them on their merits. A JSON `metrics` line with `"skipped": true` after a commit needs no action.

## Implementation notes

- **Font preload** (`App.razor`): add `<link rel="preload" href="fonts/fraunces-latin.woff2" as="font" type="font/woff2" crossorigin />` in the `<head>` before the `app.css` stylesheet link. Use exactly the un-fingerprinted relative URL that `app.css` requests (`url('fonts/fraunces-latin.woff2')`, resolved against `<base href="/">`), NOT `@Assets[...]`: a fingerprinted URL would be a different resource and the font would download twice. Preload only Fraunces (the hero display font, the LCP text); the body and mono fonts are not render-critical. Add a one-line comment saying why the URL is not fingerprinted.
- **Delegated click handler** (`site.js`): inside the existing IIFE, replace the two exported globals with private functions `toggleNav(button)` and `scrollProjects(direction)` (same bodies), and add ONE document-level listener: `document.addEventListener('click', function (event) { var target = event.target.closest('[data-action]'); if (!target) { return; } switch (target.dataset.action) { case 'toggle-nav': toggleNav(target); break; case 'toggle-theme': if (typeof window.__toggleTheme === 'function') { window.__toggleTheme(); } break; case 'scroll-projects': scrollProjects(parseInt(target.dataset.direction, 10) || 1); break; } });` (adapt to the file's style and its existing comments; keep the enhanced-load and mutation-observer code untouched). A document-level listener survives Blazor enhanced navigation DOM swaps, which is the point; say so in a comment. `__toggleTheme` stays in `theme.js` because it must run before first paint and is also used by the existing `__applyTheme` flow; do not move it. Remove `window.__toggleNav` and `window.__scrollProjects` once nothing references them (grep `src/` and `tests/` to confirm; the plan says they go in this phase when unreferenced).
- **Markup**: `MainLayout.razor`: `.nav-burger` gets `data-action="toggle-nav"` and loses `onclick`; `.theme-toggle` gets `data-action="toggle-theme"` and loses `onclick`. `Projects.razor`: the two `.carousel-btn` buttons get `data-action="scroll-projects" data-direction="-1"` and `data-direction="1"` and lose `onclick`. Keep every other attribute (`type`, `aria-*`, `title`, `class`) exactly as it is. After this there must be no `onclick="` attribute left in any `.razor` file under `src/Portfolio.Web/Components` (Blazor `@onclick` directives on admin pages are a different thing and stay).
- **Optional, owner's call** (the BJJ theme-toggle titles "Switch to the white gi (light theme)" / "Switch to the black belt (dark theme)"): do NOT implement; leave that plan box unticked and mention it in your report as deferred to the owner.
- **README.md**: (a) a Features bullet for the opt-in BJJ landing flavor (one or two sentences: `SITE_FLAVOR=bjj` turns the landing page into the BJJ-themed layout with the game-plan chart, rank bar, principles, the road (belt ladder and era table), the now tiles and a two-photo hero switch; everything is editable at `/admin/site`; the default landing page is unchanged when the flag is unset); (b) extend the existing "In-app site content" bullet to mention the new fields; (c) in "Configuration", extend the paragraph to say the BJJ copy fields are also overridable at `/admin/site`; (d) env-table rows: `SITE_FLAVOR` (`bjj` opts in; anything else or unset means the default landing page), `SITE_HERO_EYEBROW`, `SITE_GAME_PLAN` (exactly four `term | reading | how` lines joined with `\n`), `SITE_BELT_CAPTION`, `SITE_BELT_DEGREES` (0 to 6), `SITE_PRINCIPLES` (`maxim | reading` lines), `SITE_ERAS` (`date | belt | stripes | gym | location | role` lines, date `YYYY-MM-DD`, belt white/blue/purple/brown/black), `SITE_NOW` (`label | value` lines), `OWNER_PHOTO_FLIP_FILE` and `OWNER_PHOTO_FLIP_ALT` (the mat portrait; the hero shows the two-photo switch only when both photos exist and the flavor is `bjj`). Group rows the way the table already groups related keys; keep the table's column format; wording consistent with `.env.example`. Also mention `/owner-photo-flip` where `/owner-photo` is mentioned.
- **CONTEXT.md**: add a `### Landing page (BJJ flavor)` subsection under `## Language` after "Site identity" with entries in the file's exact format (`**Term**:` on its own line, a short definition paragraph, then `_Avoid_: ...`): **Flavor** (the `SITE_FLAVOR` switch; `Default` and `Bjj`; avoid: theme, skin, mode), **Game Plan** (the four-node chart Guard, Pass, Mount, Submit in the hero, each linking to the Principles; avoid: roadmap, funnel), **Rank Bar** (the black belt bar with its stripes; avoid: progress bar), **Belt** (the closed set white, blue, purple, brown, black with fixed colors per ADR 0002; avoid: rank color, level), **Degree** (a stripe on the black belt, 0 to 6, kept equal to the last black-belt era's stripes; avoid: dan, level), **Era** (one row of the road: date, belt, stripes, gym, location, role; avoid: milestone, timeline entry), **The Road** (the belt ladder above the era table; avoid: history, career timeline), **Now** (the label/value tiles; avoid: status, currently), and update **Owner Photo** to describe the two slots (the primary desk photo, and the optional mat photo in the Flip slot served at `/owner-photo-flip`; the hero switch appears only when both exist under the BJJ flavor; avoid: profile picture, avatar). Keep entries short and generic (no owner facts).
- **unit-test-instructions.md**: rebuild the "Coverage" table from `ls tests/Portfolio.Tests/*.cs` (every fixture file, grouped by area; the Support helpers listed under conventions), update the heading's counts with the real numbers from `dotnet test` after your changes, and remove the paragraph that says the table predates several fixtures.
- **Tests**: no new fixtures are required by the plan. If a cheap invariant is worth pinning, you may add a small test that no `.razor` file under `src/Portfolio.Web/Components` contains an inline `onclick="` attribute (a file scan; link the files the way `app.css` is linked in `tests/Portfolio.Tests/Portfolio.Tests.csproj` only if that stays simple; otherwise skip and say so). Existing tests must stay green and untouched.

## Local check (do it if you can within reasonable effort; otherwise say so)

Run the site locally (Phases 2 to 4 used `dotnet run` against an ephemeral, isolated Postgres container on a throwaway port with sample values exported for the process only; do not commit any `.env` change; stop processes by PID; clean up afterwards). Verify with the Browser tools: the burger opens and closes the mobile nav at 390px width and `aria-expanded` follows; the theme toggle switches and persists; the carousel buttons scroll on the projects page; after an enhanced navigation (home to projects to home) all three still work; the network panel shows `fonts/fraunces-latin.woff2` requested once (preload) and not a second time under a different URL. Do not spend more than about fifteen minutes on environment problems.

## Definition of done

- Every Phase 5 box in the plan is ticked except "Gate", the `aidlc-state.md`/`audit.md`/memory box, and the "Optional, owner's call" box (deferred).
- `dotnet build -warnaserror`: 0 warnings, 0 errors. `dotnet test`: all green; record the summary line and the final total (689 before this phase).
- No `onclick="` left in `src/Portfolio.Web/Components`; `__toggleNav` and `__scrollProjects` gone; `__toggleTheme` and `__applyTheme` untouched in `theme.js`.
- Commits on `perf/landing-closeout`, not pushed, no PR.

## Final report (the orchestrator reads this; the user does not see your transcript)

Reply with: (a) files created, modified, deleted, grouped by area; (b) the exact build and test summary lines and the final test total; (c) the exact preload tag and the delegated-handler summary; (d) what the README, CONTEXT.md and test instructions now say (headings and rows added); (e) every deviation from the plan and why; (f) the result of the local check or "not done"; (g) anything unsure or left undone, including the deferred owner's-call item; (h) the commit hashes.
